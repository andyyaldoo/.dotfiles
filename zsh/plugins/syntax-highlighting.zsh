# zsh-syntax-highlighting
# When you type a command, if the command exist, it will be highlighted with green
# useful because you don't need to run which <command> or try to run the command to check if it exists
# but it's not standard
source $(brew --prefix)/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
